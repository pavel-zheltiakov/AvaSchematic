// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.2",
  "name": "AvaSchematic 12.1.0-preview.2",
  "published_at": "2026-08-20T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/AvaSchematic/releases",
  "body": "# AvaSchematic 12.1.0-preview.2\n\nSymbol keys read like what they are, a part you draw yourself is a first-class part, and the drawing\nfaults found in the first day of use are fixed.\n\n## Defining your own symbol\n\nA symbol is data, not a subclass. `SymbolBuilder` draws one, `SymbolLibrary` holds it, and from the\nmoment it is registered the editor cannot tell it from the eighty that came in the box: same palette,\nsame placing, same wiring, same file.\n\n```csharp\nvar relay = SymbolBuilder.Create(\"Demo.Relay\", \"Relay\")\n    .Category(\"Your own parts\")                          // the palette heading it appears under\n    .FixedSize()                                         // a part keeps its pin pitch\n    .Rect(-20, -25, 40, 50).NoFill()                     // the body\n    .Line(-20, -5, 20, -5)                               // coil above, contacts below\n    .Rect(-12, -20, 24, 10).NoFill()                     // the coil\n    .Line(-8, 10, 8, 4)                                  // the moving contact\n    .Pin(\"A1\",  -40, -15, PortSide.Left,  20, name: \"COIL+\", number: \"1\")\n    .Pin(\"A2\",   40, -15, PortSide.Right, 20, name: \"COIL-\", number: \"2\")\n    .Pin(\"COM\", -40,  10, PortSide.Left,  20, name: \"COM\",   number: \"3\")\n    .Pin(\"NO\",   40,   4, PortSide.Right, 20, name: \"NO\",    number: \"4\")\n    .Pin(\"NC\",   40,  16, PortSide.Right, 20, name: \"NC\",    number: \"5\")\n    .Attribute(AttributeKeys.Designator, \"K?\", 0, -32)\n    .Build();\n\nlibrary.Register(relay);\n\n// From here it is an ordinary part.\ndocument.AddNodeAtPin(\"Demo.Relay\", \"A1\", new Point(0, 0));\n```\n\nEverything is in the symbol's own coordinates, around its own origin, and a pin's coordinates name its\n**tip** — the point a wire lands on, not where it meets the body. Keeping the tips on round numbers is\nwhat keeps every instance of the part on the grid however it is turned.\n\nNobody draws a forty-pin chip, so a pin list is the other way in. The body, the pin spacing, the\nnumbering and the labels are worked out from the two lists, and a `null` entry leaves a gap where a\npackage's unused legs are:\n\n```csharp\nlibrary.Register(ElectronicSymbols.CreateIc(\"Demo.MCU\", \"STM32G031\",\n    leftPins:  new[] { \"VDD\", \"VSS\", \"NRST\", \"PA0\", \"PA1\" },\n    rightPins: new[] { \"PA2\", \"PA3\", \"PB0\", \"SWDIO\", \"SWCLK\" },\n    bodyWidth: 120));\n```\n\nThree things worth knowing before you rely on it:\n\n- **The key is yours.** Group it the way the shipped ones are grouped — `Demo.Relay`, `Acme.Sensor` —\n  and it will not collide with a symbol added to the library later.\n- **Register before you load.** A document stores keys, not drawings. A node whose key is not in the\n  library keeps its key rather than throwing, so nothing is lost from the file, but nothing is drawn\n  either until the symbol is registered.\n- **A library serialises too.** `SchematicJson.SaveLibrary` / `LoadLibrary` turn a whole set of parts\n  into a data file, if you would rather ship parts than compile them.\n\nThe demo has both parts, in the palette under **Your own parts**, beside the built-in ones; the\n**Draw your own part** button prints the source that made them, read out of the file that compiles.\n\n## Changed\n\n- **Every symbol key is now `Group.Name`** — `Symbols.Electric.LED`, `Symbols.Flow.Decision`,\n  `Symbols.Basic.RoundedRectangle` — in one nested catalogue, grouped by what a symbol is *for*\n  rather than by which class draws it. The old `el/led` style keys are gone. **This breaks documents\n  saved with `12.1.0-preview.1`**, which was published the same day; if you have one, the keys inside\n  it are plain strings and a find-and-replace is the whole migration.\n- **The keys are `static readonly`, not `const`.** A `const` is copied into the calling assembly at\n  compile time, so a key renamed here would go on resolving to the old string in every application\n  built against the old package until each one was recompiled — and the failure would be a symbol\n  that silently is not found.\n- `SymbolLibrary.Alias(name, key)` gives an existing symbol a second name. `Symbols.Mindmap.*` are\n  the shipped example: a mindmap bubble *is* a capsule, and drawing it twice would be two things to\n  keep in step.\n\n## Fixed\n\n- **A part is solid for its own wires too.** A wire attached to a part was exempt from that part's\n  body entirely, so it could run straight across a chip to reach a pin on the far side — obvious the\n  moment a part was rotated. Only the clearance a stranger keeps is waived now, because a pin sits on\n  the body's own edge.\n- **A stub pushes further out when its lane is taken.** Pins on one edge of a part all push their\n  stubs to the same line, so several wires leaving one part wanted the same lane and no elbow shape\n  could offer another.\n- **A hand-edited line never doubles back on itself.** Dragging a run one way and then back left the\n  wire drawn out and straight back along its own line — at any zoom, a long thin rectangle shooting\n  off across the sheet. Four separate faults behind it, from a drag that moved the run to the pointer\n  rather than by the pointer, to a bend that was set to the pointer with its neighbours left behind.\n- **A dragged run stops at a part** rather than being dropped across it, and the whole line is judged\n  rather than only the run in hand — the runs either side stretch to follow it.\n- **Turning is held to the rotation step** on every path, including the rotate handle, which was\n  free-hand at fifteen degrees.\n- The polarised capacitor's curved plate was centred on the wrong side and crossed the straight one;\n  the LED's second emission arrow grew out of the first one's head. Both redrawn.\n- The JSON serializer is source-generated, so the library survives trimming and ahead-of-time\n  compilation. It previously reached its own DTOs by name, which a trimmer removes — files would then\n  save as `{}` without failing.\n\n## Beta\n\n`12.1.0-preview.2` is a prerelease and needs `--prerelease` to install. The API may change before\n12.1.0 final.\n\nDocs: https://pavel-zheltiakov.github.io/AvaSchematic/"
}];
